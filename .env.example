# -------------------------
# Project: Project-Compose-Inkwell
# Purpose: Local SNMP printer monitor w/ Prometheus + Alertmanager via Tailscale
# -------------------------

# ----- General -----
STACK_NAME=Compose-Inkwell
TIMEZONE=UTC

# ----- Tailscale -----
TS_AUTHKEY=tskey-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TS_HOSTNAME=scribe-ts
# Optional extra args (example: advertise to tag:container)
# TS_EXTRA_ARGS=--advertise-tags=tag:container --reset

# ----- Printer Targets -----
# Comma-separated list of printers to poll via SNMP
PRINTER_TARGETS=HP-LaserJet.local:161,HP-OfficeJet.local:161

# SNMP Settings
SNMP_COMMUNITY=public
SNMP_VERSION=2c
# SNMP_VERSION options: 1 | 2c | 3

# ----- Alertmanager -----
# Optional: webhook URL for notifications (Discord, Slack, n8n, etc.)
ALERT_WEBHOOK_URL=

# ----- Prometheus -----
# Data retention (default 15 days)
PROM_RETENTION=15d

# ----- Networking -----
# Ports exposed on Tailscale node
PROM_PORT=9090
AM_PORT=9093
SNMP_EXP_PORT=9116

# -------------------------
# Notes:
# - TS_AUTHKEY is required for first launch (ephemeral or reusable key).
# - ALERT_WEBHOOK_URL can point to any service that accepts POST JSON alerts.
# - PRINTER_TARGETS should resolve inside your Tailscale or LAN.
# - Adjust SNMP_COMMUNITY if your printers use custom read-only strings.
# -------------------------
